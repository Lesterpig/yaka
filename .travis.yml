language: java
install:
  - "sudo apt-get install javacc"
script:
  - "make javacc"
  - "make test"
jdk:
  - oraclejdk8
  - oraclejdk7
  - openjdk7

matrix:
  allow_failures:
    - jdk: oraclejdk7
    - jdk: openjdk7
  fast_finish: true

notifications:
  email: false