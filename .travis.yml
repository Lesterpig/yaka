language: java
install:
  - "sudo apt-get install javacc dosemu"
script:
  - "make"
  #- "make test"
  - "./travis/configure-layout.sh"
  - "make download-tasm"
  - "make test-asm FILE=example/exp.yaka"
  - "make test-asm FILE=example/function.yaka"
jdk:
  - oraclejdk8
  - oraclejdk7
  - openjdk7

matrix:
  allow_failures:
    - jdk: oraclejdk7
    - jdk: openjdk7
  fast_finish: true

notifications:
  email: false
